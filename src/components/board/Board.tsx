import { IBoard } from '../../models/IBoard';
import './board.css';

export const Board = (props: {board: IBoard}) => {

    return (
        <div className="board">
            {

            }
        </div>
    )
}